#Define the database connection(s) using the appropriate URLs and ports. Note: for this to work, you must omit the
#connection URL(s) from the database definitions in portofino-model.xml.
#The templates follow.
#Postgres:
#portofino.database.<database-name>.url=jdbc:postgresql://${env:POSTGRESQL_PORT_5432_TCP_ADDR}/<database-name>
#MySQL:
#portofino.database.<database-name>.url=jdbc:mysql://${env:MYSQL_PORT_3306_TCP_ADDR}/<database-name>
