import { MainContentDirective } from './content.directive';

describe('ContentDirective', () => {
  it('should create an instance', () => {
    const directive = new MainContentDirective(null);
    expect(directive).toBeTruthy();
  });
});
