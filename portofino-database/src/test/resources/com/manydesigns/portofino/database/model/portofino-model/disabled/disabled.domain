import com.manydesigns.portofino.model.annotations.Enabled;
import com.manydesigns.portofino.database.model.annotations.JDBCConnection;

@JDBCConnection(password = "manydesigns", driver = "org.h2.Driver", username = "manydesigns", url = "jdbc:h2:mem:hibernatetest;DB_CLOSE_DELAY=-1")
@Enabled(false)
domain disabled {
    domain PUBLIC;
}